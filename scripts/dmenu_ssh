#!/usr/bin/env bash

set -e -o pipefail -o nounset

. "$HOME/.dmenurc"
DMENU_ARGS="-i $DMENU_CONF -l 15"

ssh="$(give_me_hostname | dmenu ${DMENU_ARGS} -p 'ssh to :' "$@")"

[ "$ssh" ] && . "$HOME/.dotfiles/keychain" && termite --exec "$HOME/.dotfiles/bin/s $ssh" &
