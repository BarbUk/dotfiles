set -g default-shell /bin/bash
set -g default-command $SHELL

set-option -g prefix 'C-b'
set-option -g prefix2 'C-\'
set -g default-terminal "screen-256color"
set-window-option -g xterm-keys on
set -s escape-time 10                     # faster command sequences
set -sg repeat-time 600                   # increase repeat timeout
set -s focus-events on

set -g mouse off

if-shell -b '[[ $(echo "$TMUX" | awk -F"[/-]" "{print \$3}") == "tmux" ]]' 'set -g allow-passthrough on; set -g pane-border-lines heavy'

# boost history
set -g history-limit 150000

set -ga terminal-overrides ',xterm-256color:Tc:xterm*:smcup@:rmcup@'
set -ga terminal-overrides ',xterm*:smcup@:rmcup@'
set -ga terminal-overrides ',termite:smcup@:rmcup@'

# split panes using | and -
bind | split-window -h
bind - split-window -v
bind-key j command-prompt -p "create pane from:"  "join-pane -bh -s ':%%'"

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

#+---------+
#+ Options +
#+---------+
set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename on   # rename window to reflect current program
setw -g automatic-rename-format "#{b:pane_current_path}"
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title

set -g display-panes-time 800 # slightly longer pane indicators display time
set -g status-interval 2
set -g status on

#+--------+
#+ Status +
#+--------+
#+--- Layout ---+
set -g status-position bottom
set -g status-justify left

#+--- Colors ---+
set -g status-style bg=black,fg=white,none

#+--- Bars ---+
set -g status-left ""
set -g status-right "#[align=left]#(~/.config/tmux/plugins/git-pane-border --pane-current-path=#{pane_current_path} --pane-active=#{pane_active})#[align=right]#(~/.config/tmux/plugins/tmux-mem-cpu-load --colors --graph-lines 6 --powerline-right --cpu-mode 0 --interval 2) #[fg=brightblack]#[fg=white,bold,bg=brightblack] %H:%M #[fg=cyan,bg=brightblack,nobold,noitalics,nounderscore]#[fg=black,bg=cyan]#{hostname_short} "
set -g status-right-length 450

#+--- Windows ---+
set -g window-status-format "#[fg=black,bg=brightblack,nobold,noitalics,nounderscore]#[fg=white,bg=brightblack]#I #[fg=white,bg=brightblack,nobold,noitalics,nounderscore] #[fg=white,bg=brightblack]#W #F #[fg=brightblack,bg=black,nobold,noitalics,nounderscore]"
set -g window-status-current-format "#[fg=black,bg=cyan,nobold,noitalics,nounderscore] #[fg=black,bg=cyan]#I #[fg=black,bg=cyan,nobold,noitalics,nounderscore] #[fg=black,bg=cyan]#W #F #[fg=cyan,bg=black,nobold,noitalics,nounderscore]"
set -g window-status-separator ""

#+-------+
#+ Panes +
#+-------+
# set -g pane-border-status bottom
# set -g pane-border-format '#(~/.config/tmux/plugins/git-pane-border --pane-current-path=#{pane_current_path} --pane-active=#{pane_active} --display-only-pane-active=0)'
#set inactive/active window styles
set -g window-style 'fg=default,bg=default'
set -g window-active-style 'fg=default,bg=default'
# set the pane border colors
set -g pane-border-style 'fg=colour235'
set -g pane-active-border-style 'fg=colour137'
set -g display-panes-colour default
set -g display-panes-active-colour brightblack
# Auto hide pane-border-status line when only one pane is displayed (see tmux/tmux#566)
# set-hook -g 'after-new-window'   "if-shell -F '#{==:#{window_panes},1}' 'set -g pane-border-status off' 'set -g pane-border-status bottom'"
# set-hook -g 'after-kill-pane'    "if-shell -F '#{==:#{window_panes},1}' 'set -g pane-border-status off' 'set -g pane-border-status bottom'"
# set-hook -g 'pane-exited'        "if-shell -F '#{==:#{window_panes},1}' 'set -g pane-border-status off' 'set -g pane-border-status bottom'"
# set-hook -g 'after-split-window' "if-shell -F '#{==:#{window_panes},1}' 'set -g pane-border-status off' 'set -g pane-border-status bottom'"
# set-hook -g 'after-resize-pane'  "if-shell -F '#{||:#{==:#{window_panes},1},#{window_zoomed_flag}}' 'set -g pane-border-status off' 'set -g pane-border-status bottom'"
# set-hook -g client-focus-in "set pane-border-status bottom"
# set-hook -g client-focus-out "set pane-border-status bottom"

#+------------+
#+ Clock Mode +
#+------------+
setw -g clock-mode-colour cyan

#+----------+
#+ Messages +
#+---------+
set -g message-style fg=white,bold,bg=black
set -g message-command-style fg=white,bold,bg=black

set -g update-environment -r

setenv -g SSH_AUTH_SOCK $HOME/.ssh/ssh_auth_sock

run-shell ~/.config/tmux/plugins/sensible.tmux
run-shell ~/.config/tmux/plugins/hostname.tmux
